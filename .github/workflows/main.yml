name: Automatisation des tests
on: push
jobs:
  Connexion:
    runs-on: ubuntu-latest
    steps:
      - name: Phase de compilation
        uses: appleboy/ssh-action@master
        with:
          script: |
            cd $HOME/www/
